'use client'

/**
 * Ambient audio layer. Currently disabled — the particle-driven audio system
 * has been archived. This component renders nothing and performs no work.
 *
 * Kept as a stub so SceneRoot's import does not break. If ambient audio is
 * re-enabled in the future, implement the update loop here.
 */
export function AmbientAudio({ reduced = false }: { reduced?: boolean }) {
  return null
}
